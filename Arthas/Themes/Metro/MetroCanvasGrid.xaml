<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:Metro="clr-namespace:Arthas.Controls.Metro">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Arthas;component/Themes/Metro/MetroBase.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type Metro:MetroCanvasGrid}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Background" Value="{StaticResource Brush}" />
        <Setter Property="GridOpacity" Value="0.2" />
        <Setter Property="GridSize" Value="6" />
        <Setter Property="Viewport" Value="0,0,1,1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Metro:MetroCanvasGrid}">
                    <Grid>
                        <Border CornerRadius="{TemplateBinding CornerRadius}">
                            <Border.Background>
                                <VisualBrush Viewport="{Binding Viewport, RelativeSource={RelativeSource TemplatedParent}}" TileMode="Tile" Opacity="{Binding GridOpacity, RelativeSource={RelativeSource TemplatedParent}}">
                                    <VisualBrush.Visual>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="{Binding GridSize, RelativeSource={RelativeSource TemplatedParent}}" />
                                                <ColumnDefinition Width="{Binding GridSize, RelativeSource={RelativeSource TemplatedParent}}" />
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="{Binding GridSize, RelativeSource={RelativeSource TemplatedParent}}" />
                                                <RowDefinition Height="{Binding GridSize, RelativeSource={RelativeSource TemplatedParent}}" />
                                            </Grid.RowDefinitions>
                                            <Rectangle Grid.Column="0" Grid.Row="0" x:Name="rect" Fill="{Binding Background, RelativeSource={RelativeSource TemplatedParent}}" />
                                            <Rectangle Grid.Column="1" Grid.Row="0" />
                                            <Rectangle Grid.Column="0" Grid.Row="1" />
                                            <Rectangle Grid.Column="1" Grid.Row="1" Fill="{Binding Fill,ElementName=rect}" />
                                        </Grid>
                                    </VisualBrush.Visual>
                                </VisualBrush>
                            </Border.Background>
                        </Border>
                        <ContentPresenter />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>